# Work Log - 2026-03-22

## Infrastructure and MCP

- Added Codex MCP server entries for Supabase, Vercel, and GitHub in the local Codex config.
- Verified live MCP access for:
  - Vercel via team listing
  - Supabase via project URL lookup
  - GitHub via authenticated account lookup
- Stored GitHub MCP authentication through the `GITHUB_PAT_TOKEN` environment variable.

## Local project setup

- Installed missing frontend dependencies for `dashboard/`.
- Verified the React dashboard builds successfully with Vite.
- Added a lightweight static server for `template/Dashboard/code.html`:
  - `serve-template-dashboard.js`
  - root script: `npm run template:dashboard`

## UI changes

- Removed the progress bar and percentage indicator from the live React Location Setup page in `dashboard/src/pages/Dashboard.jsx`.
- Removed the progress UI from the static template page in `template/Dashboard/code.html`.
- Cleaned up the temporary progress-tracking script from the template after the UI was removed.

## Current local run commands

### Backend

```powershell
cd C:\Users\user\Desktop\Projects\truck-parking-ai-main
npm.cmd start
```

### React dashboard

```powershell
cd C:\Users\user\Desktop\Projects\truck-parking-ai-main
npm.cmd run dev
```

### Static template dashboard

```powershell
cd C:\Users\user\Desktop\Projects\truck-parking-ai-main
npm.cmd run template:dashboard
```

## Known limitation

- This folder currently has no `.git` directory, so changes cannot be committed or pushed from this copy until the repository metadata or a cloned git remote is restored.
