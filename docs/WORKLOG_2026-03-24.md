# Work Log - 2026-03-24

## Local development environment

- Verified the frontend project lives in `dashboard/`.
- Confirmed the dev command is `npm run dev`.
- Restarted the local Vite server in a persistent shell after the environment dropped the earlier process.
- Re-verified local access on:
  - `http://127.0.0.1:5173/`
  - `http://127.0.0.1:5173/customer-management`

## Customers page MVP refinement

- Reworked `dashboard/src/pages/CustomerManagement.jsx` into a more realistic manual lot operations page.
- Renamed the visible workflow from **Drivers** to **Customers** throughout the page UI.
- Kept only staff-confirmed statuses:
  - `Expected`
  - `Checked In`
  - `Checked Out`
  - `Needs Support`
  - `Cancelled`
- Added soft warnings without implying automation:
  - `Past ETA`
  - `Past Checkout`
- Added manual status actions for:
  - check-in
  - check-out
  - expected
  - support
  - cancellation

## Layout and density tuning

- Tightened the desktop shell and page content width in:
  - `dashboard/src/components/layout/AppLayout.jsx`
  - `dashboard/src/pages/CustomerManagement.jsx`
- Reduced oversizing at 100% browser zoom by refining:
  - page padding
  - summary card density
  - search/filter spacing
  - table row height
  - detail panel spacing
- Preserved the established palette and overall design language while making the page feel more like a compact SaaS dashboard.

## Status hierarchy and summary cards

- Applied a consistent color hierarchy across badges and action states:
  - blue for `Checked In`
  - gray for `Expected`
  - red for `Needs Support`
  - neutral gray for `Cancelled`
  - warm amber for `Checked Out`
  - soft amber for warnings
- Extended that hierarchy to the top summary cards using subtle tinted treatments and accent rails.

## Detail panel and timeline improvements

- Refined the right-side action area to feel lighter and more polished without losing the compact dashboard feel.
- Reworked the activity timeline into a horizontal activity-feed layout:
  - rail on the left
  - fixed timestamp column
  - event text column
- Tuned alignment so each row reads as:
  - dot → time → event
- Increased timestamp prominence and centered the bullet/connector to the row for better scanability.

## Custom modal interactions

- Removed native browser prompt usage from the Customers page.
- Added custom in-app modals for:
  - `Assign / Change Spot`
  - `Add Customer Note`
- Added:
  - centered modal cards
  - darkened backdrop
  - `Esc` close
  - backdrop click close
  - field autofocus on open
  - validation for empty submissions
- Fixed modal focus behavior so typing no longer shifts focus away from the active input or textarea.

## Verification

- Repeatedly verified the dashboard build after major changes with:

```powershell
cd C:\Users\user\Desktop\Projects\truck-parking-ai-main\dashboard
npm.cmd run build
```

- Confirmed there are no remaining `window.prompt()`, `window.alert()`, or `window.confirm()` calls on the Customers page.

## Files changed for this work

- `dashboard/src/components/layout/AppLayout.jsx`
- `dashboard/src/pages/CustomerManagement.jsx`
- `docs/WORKLOG_2026-03-24.md`

## Commit scope

- Intentionally excluded unrelated modified files already present in the worktree.
- The commit for today should only contain the Customers page work and this worklog entry.
